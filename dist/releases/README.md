# Versioned Release Snapshots

Each release snapshot is written to `dist/releases/<corpus-version>/`.

Each version directory should contain:

- `corpus.jsonl`
- `index.json`
- `manifest.json`
- `SHA256SUMS`
